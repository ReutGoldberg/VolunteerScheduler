// will containt all sensetive data to connect to the DB
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

//set the url of the DB in HEROKU!!! from the command line when starting the project 
//e.g --> DATABASE_URL=postgresql://janedoe:mypassword@localhost:5432/mydb

DATABASE_URL="postgresql://postgres:Password@localhost:5432/localVolunteerMe"